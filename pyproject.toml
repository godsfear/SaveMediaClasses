[project]
name = "savemediaclasses"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "flet>=0.85.1",
    "httpx>=0.28.1",
    "pillow>=12.2.0",
    "pyinstaller>=6.20.0",
]

[project.optional-dependencies]
dev = [
    "pyinstaller>=6.20.0",
]
