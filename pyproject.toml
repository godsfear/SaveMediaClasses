[project]
name = "SaveMedia"
version = "1.0.0"
requires-python = ">=3.13"
dependencies = [
    "flet>=0.85.1",
    "httpx>=0.28.1",
]

[project.optional-dependencies]
dev = [
    "pillow>=12.2.0",
    "pyinstaller>=6.20.0",
    "pytest>=8.0",
]
